name: Android Build - Nukleáris verzió

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    # KURVA EGYSZERŰ: GRADLE TELEPÍTÉSE ÉS KÉSZ
    - name: Install Gradle and Fuck the Wrapper
      run: |
        echo "🎯 GRADLE 8.4 TELEPÍTÉSE WRAPPER NÉLKÜL"
        wget -q https://services.gradle.org/distributions/gradle-8.4-bin.zip
        unzip -q gradle-8.4-bin.zip
        ./gradle-8.4/bin/gradle --version
        
    - name: Build APK
      run: |
        echo "🚀 BUILD INDUL GRADLE-DIRECT PARANCCSAL"
        ./gradle-8.4/bin/gradle assembleDebug --stacktrace
        
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: app-apk
        path: app/build/outputs/apk/debug/*.apk
